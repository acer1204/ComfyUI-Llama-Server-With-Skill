[project]
name = "comfyui-llama-server-with-skill"
description = "Send images, video frames, audio and text to a llama.cpp llama-server and get a generation prompt back. Skills are importable Markdown system-prompt packs."
version = "1.1.2"
license = { file = "LICENSE" }
requires-python = ">=3.9"
dependencies = ["requests>=2.28", "Pillow>=9.0", "numpy>=1.23"]

[project.urls]
Repository = "https://github.com/acer1204/ComfyUI-Llama-Server-With-Skill"

[tool.comfy]
PublisherId = "acer1204"
DisplayName = "Llama Server With Skill"
Icon = ""
